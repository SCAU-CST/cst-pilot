# Todo

1. [ ] **lhmGpu iGPU 真机验证**：等一台带核显/无 N 卡的机器，跑 sys gpu 验证降级输出与 notice 语义。
2. [ ] 测试PE环境
3. [ ] **ls 卷根 0B 下界可读性**：卷根枚举下约 9–10 个子项显示 0B（实测有数据，如 F:\图片 58.37GB），notice 已如实标注但易误读为空目录；建议 notice 量化缺口（未覆盖子项数/合计）或对 0B 项标注「可下钻」，并查明熔断路径（wz-index 账本缺失 vs walk 预算耗尽）。
4. [ ] 需要设计WizTree自检功能

# ToWrite PRD

5. [ ] 准备一个新工具——允许模型专门写命令txt到指定路径下，便于队员复制运行——的需求文档
6. [ ] 准备统一模型配置入口的需求文档
7. [ ] 准备用户数据手机系统的需求文档
8. [x] 考虑pi-web包的引入（2026-09-10 验证通过并引入；同日移除：`@agegr/pi-web` 0.9.0 的前端工具预设会在每次新建会话时显式下发内置工具列表，绕过 settings.json 的 defaultTools 白名单，与只读约束冲突。已从依赖、启动器与文档移除）
9. [ ] 准备自制 cst-pilot-web 包：自有 Web UI 替代 pi-web 通道（Figma 设计稿 → 逻辑契约 → 实现 → npm 发包；范围裁剪与工作量评估已定，细节见讨论记录）
